# История:
# Играчите преминават нива и трупат точки.
# Трябва да намериш най-добрия по коефициент.
# Очакван изход: ('Мария', 50.0)
def best_player(players):
    best_name = None
    best_ratio = 0

    for name, data in players.items():
        # data = {'нива': ..., 'точки': ...}
        if data['нива'] > 0:  # за да избегнем деление на 0
            ratio = data['точки'] / data['нива']
            if ratio > best_ratio:
                best_ratio = ratio
                best_name = name

    return best_name, round(best_ratio, 2)


# Пример
players = {
    'Иван': {'нива': 5, 'точки': 200},
    'Мария': {'нива': 2, 'точки': 100},
    'Гошо': {'нива': 4, 'точки': 150}
}

print(best_player(players))
# 👉 ('Мария', 50.0)

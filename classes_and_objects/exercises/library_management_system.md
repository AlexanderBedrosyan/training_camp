# Изпитна задача: Система за управление на библиотеки и книги

## Python OOP

Годината е 2025. Големите библиотечни мрежи се управляват чрез централизирана софтуерна система, която автоматизира обработката на книги, читалните зали и заемането на книги. Вие сте софтуерен инженер, отговарящ за система, която управлява различни типове книги и библиотеки. Вашата задача е да реализирате цялостна OOP система за управление на тези библиотеки.

## Общи изисквания

• Не променяйте имената на файловете и папките.
• Следвайте принципите на ООП – наследяване, капсулация и абстракция.
• Разрешено е добавянето на помощни методи и атрибути, стига да не се променя крайният резултат.

## Структура на проекта

```
project/
│
├── books/
│   ├── base_book.py
│   ├── fiction_book.py
│   └── academic_book.py
│
├── libraries/
│   ├── base_library.py
│   ├── public_library.py
│   └── university_library.py
│
├── library_management_app.py
```

---

## 1. Клас BaseBook

Абстрактен базов клас за всички книги.

### Атрибути:
• `title`: str (не може да е празен)
• `author`: str (не може да е празен)
• `copies`: int (брой налични копия)

### Методи:

#### `__init__(title: str, author: str, copies: int)`
Инициализира всички атрибути.

Валидации:
• Ако `title` е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
  ```
  "Book title cannot be empty!"
  ```
• Ако `author` е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
  ```
  "Book author cannot be empty!"
  ```
• Ако `copies` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Book copies cannot be negative!"
  ```

#### `@abstractmethod`
#### `book_type() → str`
Абстрактен метод, който всяка конкретна книга е длъжна да имплементира.

Цел:
• Да връща типа на книгата като стринг (например "FictionBook" или "AcademicBook").

#### `borrow(amount: int) → str`
Заема определен брой копия от книгата.

Поведение:
• Ако `amount` е по-голямо от наличните копия, да се вдигне `Exception` със съобщение:
  ```
  "Not enough copies available!"
  ```
• В противен случай:
  - Намалява `copies` с подаденото количество
  - Връща съобщение:
    ```
    "Borrowed {amount} cop{'y' if amount == 1 else 'ies'} of {book_title}."
    ```

#### `return_book(amount: int) → str`
Връща заети копия на книгата.

Поведение:
• Увеличава `copies` с подаденото количество
• Връща съобщение:
  ```
  "Returned {amount} cop{'y' if amount == 1 else 'ies'} of {book_title}."
  ```

#### `__str__() → str`
Връща информация за книгата.

Формат:
```
Book: {book_title} by {author} | Type: {book_type} | Copies: {copies}
```

---

## 2. Клас FictionBook

Наследява `BaseBook`.

### Характеристики:
• Начален брой копия: 20
• Има допълнителен атрибут `genre` (жанр на книгата)

### Имплементация:

#### `__init__(title: str, author: str, genre: str)`
• Подава брой копия 20 към базовия клас
• Инициализира `genre`

Валидация:
• Ако `genre` е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
  ```
  "Book genre cannot be empty!"
  ```

#### `book_type() → str`
• Връща `"FictionBook"`

#### `__str__() → str`
• Разширява базовия метод с добавяне на жанра:
  ```
  Book: {book_title} by {author} | Type: {book_type} | Genre: {genre} | Copies: {copies}
  ```

---

## 3. Клас AcademicBook

Наследява `BaseBook`.

### Характеристики:
• Начален брой копия: 10
• Има допълнителен атрибут `field` (научна област)
• При заемане се прилага лимит от максимум 2 копия на един път

### Имплементация:

#### `__init__(title: str, author: str, field: str)`
• Подава брой копия 10 към базовия клас
• Инициализира `field`

Валидация:
• Ако `field` е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
  ```
  "Book field cannot be empty!"
  ```

#### `book_type() → str`
• Връща `"AcademicBook"`

#### `borrow(amount: int) → str`
• Ако `amount` е по-голямо от 2, да се вдигне `Exception` със съобщение:
  ```
  "Cannot borrow more than 2 copies of academic books at once!"
  ```
• В противен случай извиква `super().borrow(amount)`

#### `__str__() → str`
• Разширява базовия метод с добавяне на научната област:
  ```
  Book: {book_title} by {author} | Type: {book_type} | Field: {field} | Copies: {copies}
  ```

---

## 4. Клас BaseLibrary

Абстрактен базов клас за всички видове библиотеки.

Не може да бъде инстанциран директно.

### Атрибути:
• `name`: str
  - Име на библиотеката.
  - Ако името е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Library name cannot be empty!"
    ```

• `location`: str
  - Местоположение на библиотеката.
  - Ако е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Library location cannot be empty!"
    ```

• `capacity`: int
  - Максимален брой книги, които библиотеката може да съдържа.
  - Ако капацитетът е по-малък или равен на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Library capacity must be a positive number!"
    ```

• `books`: list
  - Празен списък, съдържащ книгите в библиотеката.

### Методи:

#### `__init__(name: str, location: str, capacity: int)`
• Инициализира всички атрибути.

#### `@abstractmethod`
#### `library_type() → str`
Абстрактен метод, който всяка конкретна библиотека е длъжна да имплементира.

Цел:
• Да връща типа на библиотеката като стринг (например "PublicLibrary" или "UniversityLibrary").
• Използва се при:
  - визуализация (`__str__`)
  - проверки за съвместимост
  - логика в контролния клас

#### `add_book(book: BaseBook) → str`
Добавя книга към библиотеката.

Поведение:
• Ако библиотеката няма капацитет, да се вдигне `Exception` със съобщение:
  ```
  "Not enough capacity in the library!"
  ```
• В противен случай:
  - Добавя книгата към списъка `books`
  - Връща съобщение:
    ```
    "Book {book_title} added to {library_name}."
    ```

#### `remove_book(book_title: str) → str`
Премахва книга от библиотеката.

Поведение:
• Ако книгата не съществува в библиотеката, да се вдигне `Exception` със съобщение:
  ```
  "Book {book_title} not found in {library_name}!"
  ```
• В противен случай:
  - Премахва книгата от списъка
  - Връща съобщение:
    ```
    "Book {book_title} removed from {library_name}."
    ```

#### `details() → str`
Връща информация за библиотеката.

Формат:
```
{library_name} ({library_type}) at {location}:
Books: {book_title1}, {book_title2}, ..., {book_titleN}
```

• Ако няма книги:
```
Books: none
```

---

## 5. Клас PublicLibrary

Наследява `BaseLibrary`.

### Характеристики:
• Капацитет: 100 книги
• Използва се само за `FictionBook`

### Имплементация:

#### `__init__(name: str, location: str)`
• Подава капацитет 100 към базовия клас.

#### `library_type() → str`
• Връща `"PublicLibrary"`

---

## 6. Клас UniversityLibrary

Наследява `BaseLibrary`.

### Характеристики:
• Капацитет: 50 книги
• Използва се само за `AcademicBook`

### Имплементация:

#### `__init__(name: str, location: str)`
• Подава капацитет 50 към базовия клас.

#### `library_type() → str`
• Връща `"UniversityLibrary"`

---

## 7. Клас LibraryManagementApp

Класът `LibraryManagementApp` е основният контролен център на системата. Той отговаря за създаването и управлението на книги, библиотеки, разпределянето им, заемането и изчисляването на общия брой копия в библиотеките.

### Структура

Класът трябва да има следните атрибути:
• `books`: list
  - Празен списък, който съдържа всички създадени книги (обекти).

• `libraries`: list
  - Празен списък, който съдържа всички създадени библиотеки (обекти).

### Методи:

#### `__init__()`
Инициализира празните колекции за книги и библиотеки.

---

#### `add_book(book_type: str, title: str, author: str, *args)`
Методът създава книга от подадения тип и я добавя към системата.

Изисквания:
• Всички заглавия на книги са уникални.
• Позволени типове книги са:
  - `"FictionBook"`
  - `"AcademicBook"`

Поведение:
• Ако подаденият `book_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid book type!"
  ```
• В противен случай:
  - Създава се съответната книга
  - За `FictionBook`: `FictionBook(title, author, genre)` където `genre` е от `args`
  - За `AcademicBook`: `AcademicBook(title, author, field)` където `field` е от `args`
  - Добавя се към списъка `books`
  - Връща се съобщение:
    ```
    "{book_type} is successfully added."
    ```

---

#### `add_library(library_type: str, name: str, location: str)`
Методът създава библиотека от подадения тип и я добавя към системата.

Изисквания:
• Всички имена на библиотеки са уникални.
• Позволени типове библиотеки:
  - `"PublicLibrary"`
  - `"UniversityLibrary"`

Поведение:
• Ако подаденият `library_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid library type!"
  ```
• В противен случай:
  - Създава се съответната библиотека
  - Добавя се към списъка `libraries`
  - Връща се съобщение:
    ```
    "{library_type} is successfully added."
    ```

---

#### `add_book_to_library(book_title: str, library_name: str)`
Методът разпределя книга към библиотека (добавя я в каталога на библиотеката).

Изисквания:
• Книгата и библиотеката винаги съществуват в системата.
• Всеки тип книга може да бъде добавян само към определен тип библиотека:
  - `FictionBook` → `PublicLibrary`
  - `AcademicBook` → `UniversityLibrary`

Поведение:
• Ако книгата не е съвместима с библиотеката, методът връща:
  ```
  "Incompatible book type for this library."
  ```
• Ако библиотеката няма капацитет, се вдига `Exception` със съобщение:
  ```
  "Not enough capacity for this book!"
  ```
• При успешно добавяне:
  - Книгата се премахва от списъка `books`
  - Добавя се към библиотеката
  - Връща се съобщение:
    ```
    "Successfully added {book_title} to {library_name}."
    ```

---

#### `borrow_book(library_name: str, book_title: str, amount: int)`
Методът заема определен брой копия от книга, намираща се в дадена библиотека.

Изисквания:
• Библиотеката винаги съществува.
• Книгата винаги съществува в библиотеката.

Поведение:
• Извиква метода `borrow(amount)` на книгата
• Връща резултата от метода `borrow()`

---

#### `return_book(library_name: str, book_title: str, amount: int)`
Методът връща определен брой копия от книга в дадена библиотека.

Изисквания:
• Библиотеката винаги съществува.
• Книгата винаги съществува в библиотеката.

Поведение:
• Извиква метода `return_book(amount)` на книгата
• Връща резултата от метода `return_book()`

---

#### `total_copies(library_name: str) → str`
Методът изчислява общия брой копия на книги в дадена библиотека.

Изисквания:
• Библиотеката винаги съществува.

Поведение:
• Сумира копията на всички книги в библиотеката:
  ```
  total = sum(book.copies for book in library.books)
  ```
• Връща резултата:
  ```
  "Total copies in {library_name}: {total}"
  ```

---

#### `__str__() → str`
Методът връща информация за всички библиотеки в системата.

Формат:
```
{library_name1} ({library_type1}) at {location1}:
Books: {book_title1}, {book_title2}, ..., {book_titleN}
{library_name2} ({library_type2}) at {location2}:
Books: none
...
```

---

## Примерен изход при тестване

```python
# Създаване на приложението
app = LibraryManagementApp()

# Добавяне на книги
print(app.add_book("FictionBook", "1984", "George Orwell", "Dystopian"))
# FictionBook is successfully added.

print(app.add_book("AcademicBook", "Introduction to Algorithms", "Thomas Cormen", "Computer Science"))
# AcademicBook is successfully added.

# Добавяне на библиотеки
print(app.add_library("PublicLibrary", "City Library", "Sofia"))
# PublicLibrary is successfully added.

print(app.add_library("UniversityLibrary", "Tech University Library", "Plovdiv"))
# UniversityLibrary is successfully added.

# Разпределяне на книги към библиотеки
print(app.add_book_to_library("1984", "City Library"))
# Successfully added 1984 to City Library.

print(app.add_book_to_library("Introduction to Algorithms", "Tech University Library"))
# Successfully added Introduction to Algorithms to Tech University Library.

# Опит за несъвместимо разпределяне
print(app.add_book_to_library("1984", "Tech University Library"))
# Incompatible book type for this library.

# Заемане на книги
print(app.borrow_book("City Library", "1984", 5))
# Borrowed 5 copies of 1984.

print(app.borrow_book("Tech University Library", "Introduction to Algorithms", 2))
# Borrowed 2 copies of Introduction to Algorithms.

# Опит за заемане на повече от 2 академични книги
try:
    print(app.borrow_book("Tech University Library", "Introduction to Algorithms", 3))
except Exception as e:
    print(e)
# Cannot borrow more than 2 copies of academic books at once!

# Връщане на книги
print(app.return_book("City Library", "1984", 3))
# Returned 3 copies of 1984.

# Общ брой копия в библиотека
print(app.total_copies("City Library"))
# Total copies in City Library: 18

print(app.total_copies("Tech University Library"))
# Total copies in Tech University Library: 8

# Информация за всички библиотеки
print(app)
# City Library (PublicLibrary) at Sofia:
# Books: 1984
# Tech University Library (UniversityLibrary) at Plovdiv:
# Books: Introduction to Algorithms
```

---

## Допълнителни указания

• Използвайте `abc.ABC` и `@abstractmethod` за абстрактните класове и методи.
• Следете внимателно форматирането на изходните съобщения.
• Валидациите трябва да вдигат точно описаните изключения със съответните съобщения.
• Не забравяйте да имплементирате всички методи и атрибути според спецификацията.
• Обърнете внимание на специалните правила:
  - `FictionBook` копия започват от 20
  - `AcademicBook` копия започват от 10
  - `AcademicBook` има лимит от 2 копия при заемане
  - Съобщенията за заемане/връщане използват "copy" за единствено число и "copies" за множествено

---

**Успех с решението! 📚**

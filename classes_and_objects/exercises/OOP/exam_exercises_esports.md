# Изпитна задача: Система за управление на киберспортни турнири

## Python OOP

Годината е 2026. Киберспортът е станал един от най-популярните спортове в света. Милиони геймъри участват в турнири с огромни награди. Вие сте софтуерен инженер, отговарящ за създаването на система за управление на играчи, турнири и състезания. Вашата задача е да реализирате цялостна OOP система за управление на киберспортни турнири.

## Общи изисквания

• Не променяйте имената на файловете и папките.
• Следвайте принципите на ООП – наследяване, капсулация и абстракция.
• Разрешено е добавянето на помощни методи и атрибути, стига да не се променя крайният резултат.

## Структура на проекта

```
project/
│
├── players/
│   ├── base_player.py
│   ├── pro_player.py
│   └── streamer_player.py
│
├── tournaments/
│   ├── base_tournament.py
│   ├── competitive_tournament.py
│   └── casual_tournament.py
│
├── esports_manager_app.py
```

---

## 1. Клас BasePlayer

Абстрактен базов клас за всички играчи.

### Атрибути:
• `nickname`: str (не може да е празен, минимум 3 символа)
• `skill_level`: int (между 1 и 100)
• `energy`: int (енергия на играча за участие в турнири)

### Методи:

#### `__init__(nickname: str, skill_level: int, energy: int)`
Инициализира всички атрибути.

Валидации:
• Ако `nickname` е празен стринг, съдържа само интервали или е по-малък от 3 символа, да се вдигне `ValueError` със съобщение:
  ```
  "Nickname must be at least 3 characters long!"
  ```
• Ако `skill_level` не е в диапазона [1, 100], да се вдигне `ValueError` със съобщение:
  ```
  "Skill level must be between 1 and 100!"
  ```
• Ако `energy` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Energy cannot be negative!"
  ```

#### `@abstractmethod`
#### `player_type() → str`
Абстрактен метод, който всеки конкретен играч е длъжен да имплементира.

Цел:
• Да връща типа на играча като стринг (например "ProPlayer" или "StreamerPlayer").

#### `compete() → str`
Играчът участва в състезание.

Поведение:
• Ако `energy` е 0, да се вдигне `Exception` със съобщение:
  ```
  "{nickname} has no energy to compete!"
  ```
• В противен случай:
  - Намалява `energy` според специфичния тип играч (всеки тип има различна консумация)
  - Ако `energy` падне под 0, да остане на 0
  - Връща съобщение:
    ```
    "{nickname} competed successfully!"
    ```

#### `rest(hours: int)`
Играчът почива и възстановява енергия.

Поведение:
• Увеличава `energy` с `hours * 5`
• Ако `energy` надхвърли максималната стойност за типа играч, остава на максимума

#### `__str__() → str`
Връща информация за играча.

Формат:
```
Player: {nickname} | Type: {player_type} | Skill: {skill_level} | Energy: {energy}
```

---

## 2. Клас ProPlayer

Наследява `BasePlayer`.

### Характеристики:
• Начална енергия: 100
• При състезание (`compete()`) енергията намалява с 30
• Максимална енергия: 150

### Имплементация:

#### `__init__(nickname: str, skill_level: int)`
• Подава енергия 100 към базовия клас

#### `player_type() → str`
• Връща `"ProPlayer"`

#### `compete() → str`
• Проверява дали има енергия
• Намалява енергията с 30
• Връща съобщението за успешно участие

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако енергията надхвърли 150, задава я на 150

---

## 3. Клас StreamerPlayer

Наследява `BasePlayer`.

### Характеристики:
• Начална енергия: 80
• При състезание (`compete()`) енергията намалява с 20
• Максимална енергия: 120
• Има допълнителен атрибут `followers` (брой последователи)

### Имплементация:

#### `__init__(nickname: str, skill_level: int, followers: int)`
• Подава енергия 80 към базовия клас
• Инициализира `followers`

Валидация:
• Ако `followers` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Followers count cannot be negative!"
  ```

#### `player_type() → str`
• Връща `"StreamerPlayer"`

#### `compete() → str`
• Проверява дали има енергия
• Намалява енергията с 20
• Увеличава `followers` с 50 (печели нови последователи от участието)
• Връща съобщението за успешно участие

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако енергията надхвърли 120, задава я на 120

#### `stream(hours: int) → str`
Играчът стриймва и печели последователи.

Поведение:
• Увеличава `followers` с `hours * 100`
• Намалява `energy` с `hours * 10`
• Ако енергията падне под 0, остава на 0
• Връща съобщение:
  ```
  "{nickname} streamed for {hours} hours and gained {hours * 100} followers!"
  ```

---

## 4. Клас BaseTournament

Абстрактен базов клас за всички турнири.

Не може да бъде инстанциран директно.

### Атрибути:
• `name`: str
  - Име на турнира.
  - Ако името е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Tournament name cannot be empty!"
    ```

• `prize_pool`: float
  - Награден фонд на турнира.
  - Ако е по-малко или равно на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Prize pool must be greater than zero!"
    ```

• `capacity`: int
  - Максимален брой играчи в турнира.
  - Ако капацитетът е по-малък или равен на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Tournament capacity must be a positive number!"
    ```

• `players`: list
  - Празен списък, съдържащ играчите, регистрирани в турнира.

### Методи:

#### `__init__(name: str, prize_pool: float, capacity: int)`
• Инициализира всички атрибути.

#### `@abstractmethod`
#### `tournament_type() → str`
Абстрактен метод, който всеки конкретен турнир е длъжен да имплементира.

Цел:
• Да връща типа на турнира като стринг (например "CompetitiveTournament" или "CasualTournament").
• Използва се при:
  - визуализация (`__str__`)
  - проверки за съвместимост
  - логика в контролния клас

#### `add_player(player: BasePlayer) → str`
Добавя играч към турнира.

Поведение:
• Ако турнирът няма капацитет, да се вдигне `Exception` със съобщение:
  ```
  "Tournament is at full capacity!"
  ```
• В противен случай:
  - Добавя играча към списъка `players`
  - Връща съобщение:
    ```
    "Player {nickname} registered for {tournament_name}."
    ```

#### `remove_player(nickname: str) → str`
Премахва играч от турнира.

Поведение:
• Ако играчът не съществува в турнира, да се вдигне `Exception` със съобщение:
  ```
  "Player {nickname} not found in {tournament_name}!"
  ```
• В противен случай:
  - Премахва играча от списъка
  - Връща съобщение:
    ```
    "Player {nickname} removed from {tournament_name}."
    ```

#### `details() → str`
Връща информация за турнира.

Формат:
```
{tournament_name} ({tournament_type}) - Prize: ${prize_pool:.2f}
Players: {nickname1}, {nickname2}, ..., {nicknameN}
```

• Ако няма играчи:
```
Players: none
```

---

## 5. Клас CompetitiveTournament

Наследява `BaseTournament`.

### Характеристики:
• Капацитет: 16 играчи
• Използва се само за `ProPlayer`
• Минимално ниво на умение за участие: 50

### Имплементация:

#### `__init__(name: str, prize_pool: float)`
• Подава капацитет 16 към базовия клас.

#### `tournament_type() → str`
• Връща `"CompetitiveTournament"`

#### `add_player(player: BasePlayer) → str`
• Проверява дали играчът е `ProPlayer`
• Проверява дали `skill_level >= 50`
• Ако не отговаря на изискванията, връща:
  ```
  "Player {nickname} does not meet the requirements for {tournament_name}."
  ```
• Иначе извиква `super().add_player(player)`

---

## 6. Клас CasualTournament

Наследява `BaseTournament`.

### Характеристики:
• Капацитет: 32 играчи
• Използва се за `StreamerPlayer`
• Няма изискване за ниво на умение

### Имплементация:

#### `__init__(name: str, prize_pool: float)`
• Подава капацитет 32 към базовия клас.

#### `tournament_type() → str`
• Връща `"CasualTournament"`

---

## 7. Клас EsportsManagerApp

Класът `EsportsManagerApp` е основният контролен център на системата. Той отговаря за създаването и управлението на играчи, турнири, регистрациите, стартирането на състезания и изчисляването на статистики.

### Структура

Класът трябва да има следните атрибути:
• `players`: list
  - Празен списък, който съдържа всички създадени играчи (обекти).

• `tournaments`: list
  - Празен списък, който съдържа всички създадени турнири (обекти).

### Методи:

#### `__init__()`
Инициализира празните колекции за играчи и турнири.

---

#### `add_player(player_type: str, nickname: str, skill_level: int, *args)`
Методът създава играч от подадения тип и го добавя към системата.

Изисквания:
• Всички никнеймове са уникални.
• Позволени типове играчи са:
  - `"ProPlayer"`
  - `"StreamerPlayer"`

Поведение:
• Ако подаденият `player_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid player type!"
  ```
• В противен случай:
  - Създава се съответният играч
  - За `ProPlayer`: `ProPlayer(nickname, skill_level)`
  - За `StreamerPlayer`: `StreamerPlayer(nickname, skill_level, followers)` където `followers` е от `args`
  - Добавя се към списъка `players`
  - Връща се съобщение:
    ```
    "{player_type} {nickname} is successfully added."
    ```

---

#### `add_tournament(tournament_type: str, name: str, prize_pool: float)`
Методът създава турнир от подадения тип и го добавя към системата.

Изисквания:
• Всички имена на турнири са уникални.
• Позволени типове турнири:
  - `"CompetitiveTournament"`
  - `"CasualTournament"`

Поведение:
• Ако подаденият `tournament_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid tournament type!"
  ```
• В противен случай:
  - Създава се съответният турнир
  - Добавя се към списъка `tournaments`
  - Връща се съобщение:
    ```
    "{tournament_type} {name} is successfully added."
    ```

---

#### `register_player_for_tournament(nickname: str, tournament_name: str)`
Методът регистрира играч за турнир.

Изисквания:
• Играчът и турнирът винаги съществуват в системата.
• Всеки тип играч може да бъде регистриран само в определен тип турнир:
  - `ProPlayer` → `CompetitiveTournament`
  - `StreamerPlayer` → `CasualTournament`

Поведение:
• Ако играчът не е съвместим с турнира, методът връща:
  ```
  "Player type incompatible with this tournament."
  ```
• Ако турнирът няма капацитет, се вдига `Exception` със съобщение:
  ```
  "Tournament is at full capacity!"
  ```
• При успешна регистрация:
  - Играчът се премахва от списъка `players`
  - Добавя се към турнира
  - Връща се съобщение:
    ```
    "Successfully registered {nickname} for {tournament_name}."
    ```

---

#### `start_tournament(tournament_name: str) → str`
Методът стартира турнир и всички играчи в него състезават.

Изисквания:
• Турнирът винаги съществува.

Поведение:
• Всеки играч в турнира извиква метода `compete()`
• Ако играчът няма енергия, той се пропуска (хваща се изключението)
• Брои се колко играчи успешно са взели участие
• Връща се съобщение:
  ```
  "Tournament {tournament_name} started with {count} participants!"
  ```

---

#### `player_statistics(nickname: str) → str`
Методът връща статистика за играч.

Изисквания:
• Играчът може да е в списъка `players` или в някой турнир.

Поведение:
• Намира играча и извиква неговия `__str__()` метод
• Ако играчът не съществува в системата, връща:
  ```
  "Player {nickname} not found!"
  ```

---

#### `tournament_prize_distribution(tournament_name: str) → str`
Методът изчислява наградата за първите 3 места в турнира.

Изисквания:
• Турнирът винаги съществува.
• Турнирът има поне 3 играчи.

Поведение:
• Играчите се сортират по `skill_level` (низходящо)
• Разпределение на наградния фонд:
  - 1-во място: 50% от `prize_pool`
  - 2-ро място: 30% от `prize_pool`
  - 3-то място: 20% от `prize_pool`
• Връща се информация:
  ```
  "Prize distribution for {tournament_name}:
  1st place: {nickname1} - ${amount1:.2f}
  2nd place: {nickname2} - ${amount2:.2f}
  3rd place: {nickname3} - ${amount3:.2f}"
  ```

---

#### `__str__() → str`
Методът връща информация за всички турнири в системата.

Формат:
```
{tournament_name1} ({tournament_type1}) - Prize: ${prize_pool1:.2f}
Players: {nickname1}, {nickname2}, ..., {nicknameN}
{tournament_name2} ({tournament_type2}) - Prize: ${prize_pool2:.2f}
Players: none
...
```

---

## Примерен изход при тестване

```python
# Създаване на приложението
app = EsportsManagerApp()

# Добавяне на играчи
print(app.add_player("ProPlayer", "Faker", 98))
# ProPlayer Faker is successfully added.

print(app.add_player("StreamerPlayer", "Ninja", 75, 5000000))
# StreamerPlayer Ninja is successfully added.

print(app.add_player("ProPlayer", "s1mple", 99))
# ProPlayer s1mple is successfully added.

# Добавяне на турнири
print(app.add_tournament("CompetitiveTournament", "Worlds Championship", 1000000.0))
# CompetitiveTournament Worlds Championship is successfully added.

print(app.add_tournament("CasualTournament", "Streamer Showdown", 50000.0))
# CasualTournament Streamer Showdown is successfully added.

# Регистриране на играчи
print(app.register_player_for_tournament("Faker", "Worlds Championship"))
# Successfully registered Faker for Worlds Championship.

print(app.register_player_for_tournament("s1mple", "Worlds Championship"))
# Successfully registered s1mple for Worlds Championship.

print(app.register_player_for_tournament("Ninja", "Streamer Showdown"))
# Successfully registered Ninja for Streamer Showdown.

# Опит за несъвместима регистрация
print(app.register_player_for_tournament("Ninja", "Worlds Championship"))
# Player type incompatible with this tournament.

# Стартиране на турнир
print(app.start_tournament("Worlds Championship"))
# Tournament Worlds Championship started with 2 participants!

# Статистика за играч
print(app.player_statistics("Faker"))
# Player: Faker | Type: ProPlayer | Skill: 98 | Energy: 70

# Разпределение на награди (изисква поне 3 играчи)
# Добавяме още един играч
app.add_player("ProPlayer", "Caps", 85)
app.register_player_for_tournament("Caps", "Worlds Championship")

print(app.tournament_prize_distribution("Worlds Championship"))
# Prize distribution for Worlds Championship:
# 1st place: s1mple - $500000.00
# 2nd place: Faker - $300000.00
# 3rd place: Caps - $200000.00

# Информация за всички турнири
print(app)
# Worlds Championship (CompetitiveTournament) - Prize: $1000000.00
# Players: Faker, s1mple, Caps
# Streamer Showdown (CasualTournament) - Prize: $50000.00
# Players: Ninja
```

---

## Допълнителни указания

• Използвайте `abc.ABC` и `@abstractmethod` за абстрактните класове и методи.
• Следете внимателно форматирането на изходните съобщения.
• Валидациите трябва да вдигат точно описаните изключения със съответните съобщения.
• Не забравяйте да имплементирате всички методи и атрибути според спецификацията.
• При сортиране на играчи по skill level използвайте: `sorted(players, key=lambda p: p.skill_level, reverse=True)`

---

**Успех с решението! 🎮🏆**
# Изпитна задача: Система за управление на музикални фестивали

## Python OOP

Годината е 2026. Музикалните фестивали са все по-популярни и привличат милиони фенове от цял свят. Вие сте софтуерен инженер, отговарящ за създаването на система за управление на артисти, фестивали и изпълнения. Вашата задача е да реализирате цялостна OOP система за управление на музикални фестивали.

## Общи изисквания

• Не променяйте имената на файловете и папките.
• Следвайте принципите на ООП – наследяване, капсулация и абстракция.
• Разрешено е добавянето на помощни методи и атрибути, стига да не се променя крайният резултат.

## Структура на проекта

```
project/
│
├── artists/
│   ├── base_artist.py
│   ├── rock_artist.py
│   └── pop_artist.py
│
├── festivals/
│   ├── base_festival.py
│   ├── stadium_festival.py
│   └── club_festival.py
│
├── festival_manager_app.py
```

---

## 1. Клас BaseArtist

Абстрактен базов клас за всички артисти.

### Атрибути:
• `stage_name`: str (не може да е празен, минимум 3 символа)
• `popularity`: int (между 1 и 100)
• `stamina`: int (издръжливост на артиста за изпълнения)

### Методи:

#### `__init__(stage_name: str, popularity: int, stamina: int)`
Инициализира всички атрибути.

Валидации:
• Ако `stage_name` е празен стринг, съдържа само интервали или е по-малък от 3 символа, да се вдигне `ValueError` със съобщение:
  ```
  "Stage name must be at least 3 characters long!"
  ```
• Ако `popularity` не е в диапазона [1, 100], да се вдигне `ValueError` със съобщение:
  ```
  "Popularity must be between 1 and 100!"
  ```
• Ако `stamina` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Stamina cannot be negative!"
  ```

#### `@abstractmethod`
#### `artist_type() → str`
Абстрактен метод, който всеки конкретен артист е длъжен да имплементира.

Цел:
• Да връща типа на артиста като стринг (например "RockArtist" или "PopArtist").

#### `perform() → str`
Артистът изпълнява.

Поведение:
• Ако `stamina` е 0, да се вдигне `Exception` със съобщение:
  ```
  "{stage_name} has no stamina to perform!"
  ```
• В противен случай:
  - Намалява `stamina` според специфичния тип артист (всеки тип има различна консумация)
  - Ако `stamina` падне под 0, да остане на 0
  - Връща съобщение:
    ```
    "{stage_name} performed successfully!"
    ```

#### `rest(hours: int)`
Артистът почива и възстановява издръжливост.

Поведение:
• Увеличава `stamina` с `hours * 5`
• Ако `stamina` надхвърли максималната стойност за типа артист, остава на максимума

#### `__str__() → str`
Връща информация за артиста.

Формат:
```
Artist: {stage_name} | Type: {artist_type} | Popularity: {popularity} | Stamina: {stamina}
```

---

## 2. Клас RockArtist

Наследява `BaseArtist`.

### Характеристики:
• Начална издръжливост: 100
• При изпълнение (`perform()`) издръжливостта намалява с 30
• Максимална издръжливост: 150

### Имплементация:

#### `__init__(stage_name: str, popularity: int)`
• Подава издръжливост 100 към базовия клас

#### `artist_type() → str`
• Връща `"RockArtist"`

#### `perform() → str`
• Проверява дали има издръжливост
• Намалява издръжливостта с 30
• Връща съобщението за успешно изпълнение

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако издръжливостта надхвърли 150, задава я на 150

---

## 3. Клас PopArtist

Наследява `BaseArtist`.

### Характеристики:
• Начална издръжливост: 80
• При изпълнение (`perform()`) издръжливостта намалява с 20
• Максимална издръжливост: 120
• Има допълнителен атрибут `fans` (брой фенове)

### Имплементация:

#### `__init__(stage_name: str, popularity: int, fans: int)`
• Подава издръжливост 80 към базовия клас
• Инициализира `fans`

Валидация:
• Ако `fans` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Fans count cannot be negative!"
  ```

#### `artist_type() → str`
• Връща `"PopArtist"`

#### `perform() → str`
• Проверява дали има издръжливост
• Намалява издръжливостта с 20
• Увеличава `fans` с 100 (печели нови фенове от изпълнението)
• Връща съобщението за успешно изпълнение

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако издръжливостта надхвърли 120, задава я на 120

#### `autograph_session(hours: int) → str`
Артистът провежда сесия за автографи и печели фенове.

Поведение:
• Увеличава `fans` с `hours * 200`
• Намалява `stamina` с `hours * 10`
• Ако издръжливостта падне под 0, остава на 0
• Връща съобщение:
  ```
  "{stage_name} had an autograph session for {hours} hours and gained {hours * 200} fans!"
  ```

---

## 4. Клас BaseFestival

Абстрактен базов клас за всички фестивали.

Не може да бъде инстанциран директно.

### Атрибути:
• `name`: str
  - Име на фестивала.
  - Ако името е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Festival name cannot be empty!"
    ```

• `ticket_price`: float
  - Цена на билета за фестивала.
  - Ако е по-малко или равно на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Ticket price must be greater than zero!"
    ```

• `capacity`: int
  - Максимален брой артисти във фестивала.
  - Ако капацитетът е по-малък или равен на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Festival capacity must be a positive number!"
    ```

• `artists`: list
  - Празен списък, съдържащ артистите, регистрирани за фестивала.

### Методи:

#### `__init__(name: str, ticket_price: float, capacity: int)`
• Инициализира всички атрибути.

#### `@abstractmethod`
#### `festival_type() → str`
Абстрактен метод, който всеки конкретен фестивал е длъжен да имплементира.

Цел:
• Да връща типа на фестивала като стринг (например "StadiumFestival" или "ClubFestival").
• Използва се при:
  - визуализация (`__str__`)
  - проверки за съвместимост
  - логика в контролния клас

#### `add_artist(artist: BaseArtist) → str`
Добавя артист към фестивала.

Поведение:
• Ако фестивалът няма капацитет, да се вдигне `Exception` със съобщение:
  ```
  "Festival is at full capacity!"
  ```
• В противен случай:
  - Добавя артиста към списъка `artists`
  - Връща съобщение:
    ```
    "Artist {stage_name} registered for {festival_name}."
    ```

#### `remove_artist(stage_name: str) → str`
Премахва артист от фестивала.

Поведение:
• Ако артистът не съществува във фестивала, да се вдигне `Exception` със съобщение:
  ```
  "Artist {stage_name} not found in {festival_name}!"
  ```
• В противен случай:
  - Премахва артиста от списъка
  - Връща съобщение:
    ```
    "Artist {stage_name} removed from {festival_name}."
    ```

#### `details() → str`
Връща информация за фестивала.

Формат:
```
{festival_name} ({festival_type}) - Ticket: ${ticket_price:.2f}
Artists: {stage_name1}, {stage_name2}, ..., {stage_nameN}
```

• Ако няма артисти:
```
Artists: none
```

---

## 5. Клас StadiumFestival

Наследява `BaseFestival`.

### Характеристики:
• Капацитет: 20 артисти
• Използва се само за `RockArtist`
• Минимално ниво на популярност за участие: 60

### Имплементация:

#### `__init__(name: str, ticket_price: float)`
• Подава капацитет 20 към базовия клас.

#### `festival_type() → str`
• Връща `"StadiumFestival"`

#### `add_artist(artist: BaseArtist) → str`
• Проверява дали артистът е `RockArtist`
• Проверява дали `popularity >= 60`
• Ако не отговаря на изискванията, връща:
  ```
  "Artist {stage_name} does not meet the requirements for {festival_name}."
  ```
• Иначе извиква `super().add_artist(artist)`

---

## 6. Клас ClubFestival

Наследява `BaseFestival`.

### Характеристики:
• Капацитет: 40 артисти
• Използва се за `PopArtist`
• Няма изискване за ниво на популярност

### Имплементация:

#### `__init__(name: str, ticket_price: float)`
• Подава капацитет 40 към базовия клас.

#### `festival_type() → str`
• Връща `"ClubFestival"`

---

## 7. Клас FestivalManagerApp

Класът `FestivalManagerApp` е основният контролен център на системата. Той отговаря за създаването и управлението на артисти, фестивали, регистрациите, стартирането на изпълнения и изчисляването на статистики.

### Структура

Класът трябва да има следните атрибути:
• `artists`: list
  - Празен списък, който съдържа всички създадени артисти (обекти).

• `festivals`: list
  - Празен списък, който съдържа всички създадени фестивали (обекти).

### Методи:

#### `__init__()`
Инициализира празните колекции за артисти и фестивали.

---

#### `add_artist(artist_type: str, stage_name: str, popularity: int, *args)`
Методът създава артист от подадения тип и го добавя към системата.

Изисквания:
• Всички сценични имена са уникални.
• Позволени типове артисти са:
  - `"RockArtist"`
  - `"PopArtist"`

Поведение:
• Ако подаденият `artist_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid artist type!"
  ```
• В противен случай:
  - Създава се съответният артист
  - За `RockArtist`: `RockArtist(stage_name, popularity)`
  - За `PopArtist`: `PopArtist(stage_name, popularity, fans)` където `fans` е от `args`
  - Добавя се към списъка `artists`
  - Връща се съобщение:
    ```
    "{artist_type} {stage_name} is successfully added."
    ```

---

#### `add_festival(festival_type: str, name: str, ticket_price: float)`
Методът създава фестивал от подадения тип и го добавя към системата.

Изисквания:
• Всички имена на фестивали са уникални.
• Позволени типове фестивали:
  - `"StadiumFestival"`
  - `"ClubFestival"`

Поведение:
• Ако подаденият `festival_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid festival type!"
  ```
• В противен случай:
  - Създава се съответният фестивал
  - Добавя се към списъка `festivals`
  - Връща се съобщение:
    ```
    "{festival_type} {name} is successfully added."
    ```

---

#### `register_artist_for_festival(stage_name: str, festival_name: str)`
Методът регистрира артист за фестивал.

Изисквания:
• Артистът и фестивалът винаги съществуват в системата.
• Всеки тип артист може да бъде регистриран само в определен тип фестивал:
  - `RockArtist` → `StadiumFestival`
  - `PopArtist` → `ClubFestival`

Поведение:
• Ако артистът не е съвместим с фестивала, методът връща:
  ```
  "Artist type incompatible with this festival."
  ```
• Ако фестивалът няма капацитет, се вдига `Exception` със съобщение:
  ```
  "Festival is at full capacity!"
  ```
• При успешна регистрация:
  - Артистът се премахва от списъка `artists`
  - Добавя се към фестивала
  - Връща се съобщение:
    ```
    "Successfully registered {stage_name} for {festival_name}."
    ```

---

#### `start_festival(festival_name: str) → str`
Методът стартира фестивал и всички артисти в него изпълняват.

Изисквания:
• Фестивалът винаги съществува.

Поведение:
• Всеки артист във фестивала извиква метода `perform()`
• Ако артистът няма издръжливост, той се пропуска (хваща се изключението)
• Брои се колко артисти успешно са изпълнили
• Връща се съобщение:
  ```
  "Festival {festival_name} started with {count} performers!"
  ```

---

#### `artist_statistics(stage_name: str) → str`
Методът връща статистика за артист.

Изисквания:
• Артистът може да е в списъка `artists` или в някой фестивал.

Поведение:
• Намира артиста и извиква неговия `__str__()` метод
• Ако артистът не съществува в системата, връща:
  ```
  "Artist {stage_name} not found!"
  ```

---

#### `festival_earnings_distribution(festival_name: str) → str`
Методът изчислява приходите за първите 3 места във фестивала.

Изисквания:
• Фестивалът винаги съществува.
• Фестивалът има поне 3 артисти.

Поведение:
• Артистите се сортират по `popularity` (низходящо)
• Приходи се базират на броя билети (всеки артист привлича различен брой посетители):
  - 1-во място: 1000 билета × `ticket_price`
  - 2-ро място: 600 билета × `ticket_price`
  - 3-то място: 400 билета × `ticket_price`
• Връща се информация:
  ```
  "Earnings distribution for {festival_name}:
  1st place: {stage_name1} - ${amount1:.2f}
  2nd place: {stage_name2} - ${amount2:.2f}
  3rd place: {stage_name3} - ${amount3:.2f}"
  ```

---

#### `__str__() → str`
Методът връща информация за всички фестивали в системата.

Формат:
```
{festival_name1} ({festival_type1}) - Ticket: ${ticket_price1:.2f}
Artists: {stage_name1}, {stage_name2}, ..., {stage_nameN}
{festival_name2} ({festival_type2}) - Ticket: ${ticket_price2:.2f}
Artists: none
...
```

---

## Примерен изход при тестване

```python
# Създаване на приложението
app = FestivalManagerApp()

# Добавяне на артисти
print(app.add_artist("RockArtist", "Metallica", 95))
# RockArtist Metallica is successfully added.

print(app.add_artist("PopArtist", "Billie", 85, 2000000))
# PopArtist Billie is successfully added.

print(app.add_artist("RockArtist", "AC/DC", 98))
# RockArtist AC/DC is successfully added.

# Добавяне на фестивали
print(app.add_festival("StadiumFestival", "Rock in Rio", 150.0))
# StadiumFestival Rock in Rio is successfully added.

print(app.add_festival("ClubFestival", "Pop Extravaganza", 75.0))
# ClubFestival Pop Extravaganza is successfully added.

# Регистриране на артисти
print(app.register_artist_for_festival("Metallica", "Rock in Rio"))
# Successfully registered Metallica for Rock in Rio.

print(app.register_artist_for_festival("AC/DC", "Rock in Rio"))
# Successfully registered AC/DC for Rock in Rio.

print(app.register_artist_for_festival("Billie", "Pop Extravaganza"))
# Successfully registered Billie for Pop Extravaganza.

# Опит за несъвместима регистрация
print(app.register_artist_for_festival("Billie", "Rock in Rio"))
# Artist type incompatible with this festival.

# Стартиране на фестивал
print(app.start_festival("Rock in Rio"))
# Festival Rock in Rio started with 2 performers!

# Статистика за артист
print(app.artist_statistics("Metallica"))
# Artist: Metallica | Type: RockArtist | Popularity: 95 | Stamina: 70

# Разпределение на приходи (изисква поне 3 артисти)
# Добавяме още един артист
app.add_artist("RockArtist", "Slipknot", 75)
app.register_artist_for_festival("Slipknot", "Rock in Rio")

print(app.festival_earnings_distribution("Rock in Rio"))
# Earnings distribution for Rock in Rio:
# 1st place: AC/DC - $150000.00
# 2nd place: Metallica - $90000.00
# 3rd place: Slipknot - $60000.00

# Информация за всички фестивали
print(app)
# Rock in Rio (StadiumFestival) - Ticket: $150.00
# Artists: Metallica, AC/DC, Slipknot
# Pop Extravaganza (ClubFestival) - Ticket: $75.00
# Artists: Billie
```

---

## Допълнителни указания

• Използвайте `abc.ABC` и `@abstractmethod` за абстрактните класове и методи.
• Следете внимателно форматирането на изходните съобщения.
• Валидациите трябва да вдигат точно описаните изключения със съответните съобщения.
• Не забравяйте да имплементирате всички методи и атрибути според спецификацията.
• При сортиране на артисти по popularity използвайте: `sorted(artists, key=lambda a: a.popularity, reverse=True)`

---

**Успех с решението! 🎸🎤🎶**

# Изпитна задача: Система за управление на футболен турнир

## Python OOP

Годината е 2026. Футболните турнири привличат милиони фенове и генерират огромни приходи. Вие сте софтуерен инженер, отговарящ за създаването на система за управление на играчи, отбори и турнири. Вашата задача е да реализирате цялостна OOP система за управление на футболен турнир с много логика и сложна структура.

## Общи изисквания

• Не променяйте имената на файловете и папките.
• Следвайте принципите на ООП – наследяване, капсулация и абстракция.
• Разрешено е добавянето на помощни методи и атрибути, стига да не се променя крайният резултат.

## Структура на проекта

```
project/
│
├── players/
│   ├── base_player.py
│   ├── striker.py
│   ├── midfielder.py
│   └── goalkeeper.py
│
├── teams/
│   ├── base_team.py
│   ├── national_team.py
│   └── club_team.py
│
├── tournament_manager_app.py
```

---

## 1. Клас BasePlayer

Абстрактен базов клас за всички играчи.

### Атрибути:
• `name`: str (не може да е празен, минимум 3 символа)
• `skill_level`: int (между 1 и 100, представлява уменията на играча)
• `energy`: int (енергията на играча за мачове, започва различно според позицията)
• `yellow_cards`: int (брой жълти картони, започва от 0)
• `red_cards`: int (брой червени картони, започва от 0)
• `goals`: int (брой голове, започва от 0)

### Методи:

#### `__init__(name: str, skill_level: int, energy: int)`
Инициализира всички атрибути.

Валидации:
• Ако `name` е празен стринг, съдържа само интервали или е по-малък от 3 символа, да се вдигне `ValueError` със съобщение:
  ```
  "Player name must be at least 3 characters long!"
  ```
• Ако `skill_level` не е в диапазона [1, 100], да се вдигне `ValueError` със съобщение:
  ```
  "Skill level must be between 1 and 100!"
  ```
• Ако `energy` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Energy cannot be negative!"
  ```

#### `@abstractmethod`
#### `position() → str`
Абстрактен метод, който всеки конкретен играч е длъжен да имплементира.

Цел:
• Да връща позицията на играча като стринг (например "Striker" или "Goalkeeper").

#### `play() → str`
Играчът участва в мач.

Поведение:
• Ако играчът е наказан (има `red_cards > 0` или `yellow_cards >= 2`), да се вдигне `Exception` със съобщение:
  ```
  "{name} is suspended and cannot play!"
  ```
• Ако `energy` е 0 или по-малко, да се вдигне `Exception` със съобщение:
  ```
  "{name} has no energy to play!"
  ```
• В противен случай:
  - Намалява `energy` според специфичната позиция (всяка позиция има различна консумация)
  - Ако `energy` падне под 0, да остане на 0
  - Увеличава `goals` според позицията (имат различна вероятност за голове)
  - Връща съобщение:
    ```
    "{name} played successfully!"
    ```

#### `rest(hours: int)`
Играчът почива и възстановява енергия.

Поведение:
• Увеличава `energy` с `hours * 10`
• Ако `energy` надхвърли максималната стойност за позицията, остава на максимума

#### `receive_card(card_type: str) → str`
Играчът получава картон.

Параметри:
• `card_type`: "yellow" или "red"

Поведение:
• Ако `card_type` е "yellow":
  - Увеличава `yellow_cards` с 1
  - Ако `yellow_cards` стане 2, играчът е наказан за следващия мач
  - Връща съобщение:
    ```
    "{name} received a yellow card!"
    ```
• Ако `card_type` е "red":
  - Увеличава `red_cards` с 1
  - Играчът е наказан за следващите 3 мача
  - Връща съобщение:
    ```
    "{name} received a red card and is suspended!"
    ```
• Ако `card_type` не е нито "yellow", нито "red", да се вдигне `ValueError` със съобщение:
  ```
  "Invalid card type!"
  ```

#### `clear_suspension()`
Изчиства наказанията на играча след изтърпяване.

Поведение:
• Ако `red_cards > 0`:
  - Намалява `red_cards` с 1
• Иначе, ако `yellow_cards >= 2`:
  - Задава `yellow_cards` на 0

#### `__str__() → str`
Връща информация за играча.

Формат:
```
Player: {name} | Position: {position} | Skill: {skill_level} | Energy: {energy} | Goals: {goals}
```

---

## 2. Клас Striker

Наследява `BasePlayer`.

### Характеристики:
• Начална енергия: 90
• При мач (`play()`) енергията намалява с 25
• Максимална енергия: 120
• При мач има 60% шанс да вкара гол (използвайте `random.randint(1, 10) > 4` за проверка)

### Имплементация:

#### `__init__(name: str, skill_level: int)`
• Подава енергия 90 към базовия клас

#### `position() → str`
• Връща `"Striker"`

#### `play() → str`
• Проверява дали може да играе (не е наказан и има енергия)
• Намалява енергията с 25
• Проверява дали вкарва гол (60% шанс)
• Ако вкара гол, увеличава `goals` с 1
• Връща съобщението за успешно изиграване

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако енергията надхвърли 120, задава я на 120

---

## 3. Клас Midfielder

Наследява `BasePlayer`.

### Характеристики:
• Начална енергия: 100
• При мач (`play()`) енергията намалява с 30
• Максимална енергия: 140
• При мач има 30% шанс да вкара гол (използвайте `random.randint(1, 10) > 7` за проверка)
• Има допълнителен атрибут `assists` (брой асистенции)

### Имплементация:

#### `__init__(name: str, skill_level: int, assists: int)`
• Подава енергия 100 към базовия клас
• Инициализира `assists`

Валидация:
• Ако `assists` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Assists count cannot be negative!"
  ```

#### `position() → str`
• Връща `"Midfielder"`

#### `play() → str`
• Проверява дали може да играе (не е наказан и има енергия)
• Намалява енергията с 30
• Проверява дали вкарва гол (30% шанс)
• Ако вкара гол, увеличава `goals` с 1
• Има 50% шанс да направи асистенция (използвайте `random.randint(1, 10) > 5`)
• Ако направи асистенция, увеличава `assists` с 1
• Връща съобщението за успешно изиграване

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако енергията надхвърли 140, задава я на 140

#### `training_session(hours: int) → str`
Футболистът тренира и подобрява уменията си.

Поведение:
• Увеличава `skill_level` с `hours * 2` (максимум 100)
• Увеличава `assists` с `hours * 3`
• Намалява `energy` с `hours * 15`
• Ако енергията падне под 0, остава на 0
• Връща съобщение:
  ```
  "{name} trained for {hours} hours and improved skills and assists!"
  ```

---

## 4. Клас Goalkeeper

Наследява `BasePlayer`.

### Характеристики:
• Начална енергия: 110
• При мач (`play()`) енергията намалява с 20
• Максимална енергия: 150
• Вратарите почти не вкарват голове (има 5% шанс - използвайте `random.randint(1, 20) == 1`)
• Има допълнителен атрибут `saves` (брой спасявания)

### Имплементация:

#### `__init__(name: str, skill_level: int, saves: int)`
• Подава енергия 110 към базовия клас
• Инициализира `saves`

Валидация:
• Ако `saves` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Saves count cannot be negative!"
  ```

#### `position() → str`
• Връща `"Goalkeeper"`

#### `play() → str`
• Проверява дали може да играе (не е наказан и има енергия)
• Намалява енергията с 20
• Проверява дали вкарва гол (5% шанс)
• Ако вкара гол, увеличава `goals` с 1
• Увеличава `saves` с броя спасявания за един мач (използвайте `random.randint(3, 10)`)
• Връща съобщение:
  ```
  "{name} played successfully and made {new_saves} saves!"
  ```

#### `rest(hours: int)`
• Извиква `super().rest(hours)`
• Ако енергията надхвърли 150, задава я на 150

---

## 5. Клас BaseTeam

Абстрактен базов клас за всички отбори.

Не може да бъде инстанциран директно.

### Атрибути:
• `name`: str
  - Име на отбора.
  - Ако името е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Team name cannot be empty!"
    ```

• `budget`: float
  - Бюджет на отбора за трансфери.
  - Ако е по-малко от 0, да се вдигне `ValueError` със съобщение:
    ```
    "Budget cannot be negative!"
    ```

• `capacity`: int
  - Максимален брой играчи в отбора.
  - Ако капацитетът е по-малък или равен на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Team capacity must be a positive number!"
    ```

• `players`: list
  - Празен списък, съдържащ играчите в отбора.

• `points`: int
  - Точки в турнира (започва от 0).

• `matches_played`: int
  - Брой изиграни мачове (започва от 0).

### Методи:

#### `__init__(name: str, budget: float, capacity: int)`
• Инициализира всички атрибути.

#### `@abstractmethod`
#### `team_type() → str`
Абстрактен метод, който всеки конкретен отбор е длъжен да имплементира.

Цел:
• Да връща типа на отбора като стринг (например "NationalTeam" или "ClubTeam").

#### `add_player(player: BasePlayer) → str`
Добавя играч към отбора.

Поведение:
• Ако отборът няма капацитет, да се вдигне `Exception` със съобщение:
  ```
  "Team is at full capacity!"
  ```
• В противен случай:
  - Добавя играча към списъка `players`
  - Връща съобщение:
    ```
    "Player {player_name} signed for {team_name}."
    ```

#### `remove_player(player_name: str) → str`
Премахва играч от отбора.

Поведение:
• Ако играчът не съществува в отбора, да се вдигне `Exception` със съобщение:
  ```
  "Player {player_name} not found in {team_name}!"
  ```
• В противен случай:
  - Премахва играча от списъка
  - Връща съобщение:
    ```
    "Player {player_name} left {team_name}."
    ```

#### `team_strength() → float`
Изчислява силата на отбора.

Формула:
• Средното `skill_level` на всички играчи × (брой играчи / капацитет)
• Ако няма играчи, връща 0.0

#### `win_match()`
Отборът печели мач.

Поведение:
• Увеличава `points` с 3
• Увеличава `matches_played` с 1

#### `draw_match()`
Отборът играе на равно.

Поведение:
• Увеличава `points` с 1
• Увеличава `matches_played` с 1

#### `lose_match()`
Отборът губи мач.

Поведение:
• Увеличава `matches_played` с 1 (без точки)

#### `details() → str`
Връща информация за отбора.

Формат:
```
{team_name} ({team_type}) - Budget: ${budget:.2f} | Points: {points}
Players: {player_name1}, {player_name2}, ..., {player_nameN}
```

• Ако няма играчи:
```
Players: none
```

---

## 6. Клас NationalTeam

Наследява `BaseTeam`.

### Характеристики:
• Капацитет: 23 играчи
• Бюджет от държавата: започва винаги от 5000000.0
• Трябва да има поне 1 вратар
• Всички играчи трябва да имат `skill_level >= 70`

### Имплементация:

#### `__init__(name: str)`
• Подава бюджет 5000000.0 и капацитет 23 към базовия клас.

#### `team_type() → str`
• Връща `"NationalTeam"`

#### `add_player(player: BasePlayer) → str`
• Проверява дали играчът има `skill_level >= 70`
• Ако не отговаря на изискването, връща:
  ```
  "Player {player_name} does not meet the skill requirements for {team_name}."
  ```
• Иначе извиква `super().add_player(player)`

#### `validate_lineup() → bool`
Проверява дали отборът има валиден състав за мач.

Изисквания:
• Минимум 11 играчи
• Поне 1 вратар
• Поне 1 нападател

Връща:
• `True` ако съставът е валиден
• `False` в противен случай

---

## 7. Клас ClubTeam

Наследява `BaseTeam`.

### Характеристики:
• Капацитет: 30 играчи
• Бюджет се определя при създаване
• Може да купува играчи чрез трансфери
• Няма специални изисквания за skill level

### Имплементация:

#### `__init__(name: str, budget: float)`
• Подава капацитет 30 към базовия клас.

#### `team_type() → str`
• Връща `"ClubTeam"`

#### `transfer_player(player: BasePlayer, price: float) → str`
Купува играч чрез трансфер.

Поведение:
• Ако `price` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Transfer price cannot be negative!"
  ```
• Ако бюджетът не е достатъчен (`budget < price`), връща:
  ```
  "Not enough budget to transfer {player_name}!"
  ```
• Иначе:
  - Намалява бюджета с `price`
  - Добавя играча към отбора (използва `add_player()`)
  - Връща съобщение:
    ```
    "{team_name} successfully transferred {player_name} for ${price:.2f}!"
    ```

---

## 8. Клас TournamentManagerApp

Класът `TournamentManagerApp` е основният контролен център на системата. Той отговаря за създаването и управлението на играчи, отбори, трансфери, провеждането на мачове и изчисляването на класиране.

### Структура

Класът трябва да има следните атрибути:
• `players`: list
  - Празен списък, който съдържа всички създадени играчи без отбор (свободни агенти).

• `teams`: list
  - Празен списък, който съдържа всички създадени отбори.

### Методи:

#### `__init__()`
Инициализира празните колекции за играчи и отбори.

---

#### `add_player(position: str, name: str, skill_level: int, *args)`
Методът създава играч от подадената позиция и го добавя към системата.

Изисквания:
• Всички имена на играчи са уникални.
• Позволени позиции:
  - `"Striker"`
  - `"Midfielder"`
  - `"Goalkeeper"`

Поведение:
• Ако подадената `position` не е валидна, се вдига `Exception` със съобщение:
  ```
  "Invalid player position!"
  ```
• В противен случай:
  - Създава се съответният играч
  - За `Striker`: `Striker(name, skill_level)`
  - За `Midfielder`: `Midfielder(name, skill_level, assists)` където `assists` е от `args`
  - За `Goalkeeper`: `Goalkeeper(name, skill_level, saves)` където `saves` е от `args`
  - Добавя се към списъка `players`
  - Връща се съобщение:
    ```
    "{position} {name} is successfully added."
    ```

---

#### `add_team(team_type: str, name: str, *args)`
Методът създава отбор от подадения тип и го добавя към системата.

Изисквания:
• Всички имена на отбори са уникални.
• Позволени типове отбори:
  - `"NationalTeam"`
  - `"ClubTeam"`

Поведение:
• Ако подаденият `team_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid team type!"
  ```
• В противен случай:
  - Създава се съответният отбор
  - За `NationalTeam`: `NationalTeam(name)`
  - За `ClubTeam`: `ClubTeam(name, budget)` където `budget` е от `args`
  - Добавя се към списъка `teams`
  - Връща се съобщение:
    ```
    "{team_type} {name} is successfully added."
    ```

---

#### `sign_player(player_name: str, team_name: str)`
Методът подписва играч за отбор.

Изисквания:
• Играчът и отборът винаги съществуват в системата.

Поведение:
• Ако отборът е `NationalTeam` и играчът не отговаря на изискванията (skill_level < 70):
  ```
  "Player {player_name} does not meet the skill requirements for {team_name}."
  ```
• Ако отборът е на пълен капацитет, се вдига `Exception` със съобщение:
  ```
  "Team is at full capacity!"
  ```
• При успешно подписване:
  - Играчът се премахва от списъка `players`
  - Добавя се към отбора
  - Връща се съобщение:
    ```
    "Successfully signed {player_name} for {team_name}."
    ```

---

#### `play_match(team1_name: str, team2_name: str) → str`
Методът провежда мач между два отбора.

Изисквания:
• И двата отбора винаги съществуват.
• И двата отбора имат валиден състав (ако са национални отбори)

Логика на мача:
• Изчислява силата на всеки отбор (`team_strength()`)
• Всеки отбор получава "мач сила" като:
  - Сила = `team_strength() + random.randint(0, 20)`
• Отборът с по-голяма "мач сила" печели
• При равенство на "мач силата" е равенство

Побочни ефекти:
• Всички играчи на двата отбора играят (`play()` метод)
• Ако играч не може да играе (наказан или няма енергия), се пропуска
• Има 20% шанс случаен играч да получи жълт картон (`random.randint(1, 5) == 1`)
• Има 5% шанс случаен играч да получи червен картон (`random.randint(1, 20) == 1`)

Поведение:
• Ако `team1` печели:
  - `team1.win_match()`, `team2.lose_match()`
  - Връща:
    ```
    "Match Result: {team1_name} won against {team2_name}!"
    ```
• Ако `team2` печели:
  - `team2.win_match()`, `team1.lose_match()`
  - Връща:
    ```
    "Match Result: {team2_name} won against {team1_name}!"
    ```
• При равенство:
  - `team1.draw_match()`, `team2.draw_match()`
  - Връща:
    ```
    "Match Result: Draw between {team1_name} and {team2_name}!"
    ```

---

#### `player_statistics(player_name: str) → str`
Методът връща статистика за играч.

Изисквания:
• Играчът може да е в списъка `players` или в някой отбор.

Поведение:
• Намира играча и извиква неговия `__str__()` метод
• Ако играчът не съществува в системата, връща:
  ```
  "Player {player_name} not found!"
  ```

---

#### `team_standings() → str`
Методът връща класирането на отборите в турнира.

Поведение:
• Сортира отборите по:
  1. Точки (низходящо)
  2. Сила на отбора (`team_strength()`) - при равни точки
  3. Име (азбучно) - при равна сила
• Връща форматирано класиране:
  ```
  "Tournament Standings:
  1. {team1_name} - {points1} points
  2. {team2_name} - {points2} points
  3. {team3_name} - {points3} points
  ..."
  ```
• Ако няма отбори:
  ```
  "No teams in the tournament!"
  ```

---

#### `top_scorers(n: int) → str`
Методът връща топ N най-резултатни играчи в турнира.

Изисквания:
• `n` е положително число

Поведение:
• Събира всички играчи от всички отбори
• Сортира ги по `goals` (низходящо)
• Взема първите `n`
• Ако `n` е по-голямо от броя играчи, взема всички налични
• Връща форматиран списък:
  ```
  "Top {n} Scorers:
  1. {player1_name} ({position1}) - {goals1} goals
  2. {player2_name} ({position2}) - {goals2} goals
  ..."
  ```
• Ако няма играчи с голове:
  ```
  "No goals scored yet!"
  ```

---

#### `__str__() → str`
Методът връща информация за всички отбори в системата.

Формат:
```
{team_name1} ({team_type1}) - Budget: ${budget1:.2f} | Points: {points1}
Players: {player_name1}, {player_name2}, ..., {player_nameN}
{team_name2} ({team_type2}) - Budget: ${budget2:.2f} | Points: {points2}
Players: none
...
```

---

## Примерен изход при тестване

```python
import random
random.seed(42)  # За предвидими резултати

# Създаване на приложението
app = TournamentManagerApp()

# Добавяне на играчи
print(app.add_player("Striker", "Cristiano", 95))
# Striker Cristiano is successfully added.

print(app.add_player("Midfielder", "Modric", 92, 50))
# Midfielder Modric is successfully added.

print(app.add_player("Goalkeeper", "Courtois", 88, 120))
# Goalkeeper Courtois is successfully added.

print(app.add_player("Striker", "Benzema", 90))
# Striker Benzema is successfully added.

# Добавяне на отбори
print(app.add_team("NationalTeam", "Spain"))
# NationalTeam Spain is successfully added.

print(app.add_team("ClubTeam", "Real Madrid", 150000000.0))
# ClubTeam Real Madrid is successfully added.

# Подписване на играчи
print(app.sign_player("Cristiano", "Real Madrid"))
# Successfully signed Cristiano for Real Madrid.

print(app.sign_player("Modric", "Real Madrid"))
# Successfully signed Modric for Real Madrid.

print(app.sign_player("Courtois", "Real Madrid"))
# Successfully signed Courtois for Real Madrid.

# Добавяне на още играчи и отбори
app.add_player("Striker", "Messi", 96)
app.add_player("Midfielder", "Busquets", 85, 40)
app.add_player("Goalkeeper", "Ter Stegen", 89, 110)

app.add_team("ClubTeam", "Barcelona", 140000000.0)
app.sign_player("Messi", "Barcelona")
app.sign_player("Busquets", "Barcelona")
app.sign_player("Ter Stegen", "Barcelona")

# Провеждане на мач
print(app.play_match("Real Madrid", "Barcelona"))
# Match Result: Real Madrid won against Barcelona!
# (или друг резултат, в зависимост от случайността)

# Статистика за играч
print(app.player_statistics("Cristiano"))
# Player: Cristiano | Position: Striker | Skill: 95 | Energy: 65 | Goals: 1

# Класиране
print(app.team_standings())
# Tournament Standings:
# 1. Real Madrid - 3 points
# 2. Barcelona - 0 points

# Топ голмайстори
print(app.top_scorers(3))
# Top 3 Scorers:
# 1. Cristiano (Striker) - 1 goals
# 2. Messi (Striker) - 0 goals
# 3. Modric (Midfielder) - 0 goals

# Информация за всички отбори
print(app)
# Spain (NationalTeam) - Budget: $5000000.00 | Points: 0
# Players: none
# Real Madrid (ClubTeam) - Budget: $150000000.00 | Points: 3
# Players: Cristiano, Modric, Courtois
# Barcelona (ClubTeam) - Budget: $140000000.00 | Points: 0
# Players: Messi, Busquets, Ter Stegen
```

---

## Допълнителни указания

• Използвайте `abc.ABC` и `@abstractmethod` за абстрактните класове и методи.
• Използвайте модула `random` за случайните елементи в играта.
• Следете внимателно форматирането на изходните съобщения.
• Валидациите трябва да вдигат точно описаните изключения със съответните съобщения.
• При сортиране използвайте `sorted()` с подходящи `key` функции:
  - За класиране: `sorted(teams, key=lambda t: (-t.points, -t.team_strength(), t.name))`
  - За голмайстори: `sorted(all_players, key=lambda p: p.goals, reverse=True)`
• Логиката на мача трябва да включва всички посочени елементи: играчи играят, получават картони, и др.
• Наказаните играчи не могат да играят докато не бъдат освободени чрез `clear_suspension()`

---

**Успех с решението! ⚽🏆🎯**

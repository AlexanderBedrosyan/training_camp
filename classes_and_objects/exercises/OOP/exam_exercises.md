# Изпитна задача: Система за управление на магазини и продукти

## Python OOP

Годината е 2025. Големите вериги магазини се управляват чрез централизирана софтуерна система, която автоматизира обработката на продукти, складовете и продажбите. Вие сте софтуерен инженер, отговарящ за система, която управлява различни типове продукти и магазини. Вашата задача е да реализирате цялостна OOP система за управление на тези магазини.

## Общи изисквания

• Не променяйте имената на файловете и папките.
• Следвайте принципите на ООП – наследяване, капсулация и абстракция.
• Разрешено е добавянето на помощни методи и атрибути, стига да не се променя крайният резултат.

## Структура на проекта

```
project/
│
├── products/
│   ├── base_product.py
│   ├── electronics_product.py
│   └── food_product.py
│
├── shops/
│   ├── base_shop.py
│   ├── tech_shop.py
│   └── grocery_shop.py
│
├── shop_management_app.py
```

---

## 1. Клас BaseProduct

Абстрактен базов клас за всички продукти.

### Атрибути:
• `name`: str (не може да е празен)
• `price`: float (по-голямо от 0)
• `quantity`: int (наличност на склад)

### Методи:

#### `__init__(name: str, price: float, quantity: int)`
Инициализира всички атрибути.

Валидации:
• Ако `name` е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
  ```
  "Product name cannot be empty!"
  ```
• Ако `price` е по-малко или равно на 0, да се вдигне `ValueError` със съобщение:
  ```
  "Product price must be greater than zero!"
  ```
• Ако `quantity` е по-малко от 0, да се вдигне `ValueError` със съобщение:
  ```
  "Product quantity cannot be negative!"
  ```

#### `@abstractmethod`
#### `product_type() → str`
Абстрактен метод, който всеки конкретен продукт е длъжен да имплементира.

Цел:
• Да връща типа на продукта като стринг (например "ElectronicsProduct" или "FoodProduct").

#### `sell(amount: int) → str`
Продава определено количество от продукта.

Поведение:
• Ако `amount` е по-голямо от наличното количество, да се вдигне `Exception` със съобщение:
  ```
  "Not enough quantity available!"
  ```
• В противен случай:
  - Намалява `quantity` с подаденото количество
  - Връща съобщение:
    ```
    "Sold {amount} units of {product_name}."
    ```

#### `restock(amount: int) → str`
Зарежда продукта с ново количество.

Поведение:
• Увеличава `quantity` с подаденото количество
• Връща съобщение:
  ```
  "Restocked {amount} units of {product_name}."
  ```

#### `__str__() → str`
Връща информация за продукта.

Формат:
```
Product: {product_name} | Type: {product_type} | Price: ${price:.2f} | Quantity: {quantity}
```

---

## 2. Клас ElectronicsProduct

Наследява `BaseProduct`.

### Характеристики:
• Начално количество: 50 единици
• При продажба се прилага 5% отстъпка на цената

### Имплементация:

#### `__init__(name: str, price: float)`
• Подава количество 50 към базовия клас

#### `product_type() → str`
• Връща `"ElectronicsProduct"`

#### `sell(amount: int) → str`
• Прилага 5% отстъпка на цената
• Извиква `super().sell(amount)`

---

## 3. Клас FoodProduct

Наследява `BaseProduct`.

### Характеристики:
• Начално количество: 100 единици
• Има допълнителен атрибут `expiration_days` (брой дни до изтичане)

### Имплементация:

#### `__init__(name: str, price: float, expiration_days: int)`
• Подава количество 100 към базовия клас
• Инициализира `expiration_days`

Валидация:
• Ако `expiration_days` е по-малко или равно на 0, да се вдигне `ValueError` със съобщение:
  ```
  "Expiration days must be positive!"
  ```

#### `product_type() → str`
• Връща `"FoodProduct"`

#### `is_expired() → bool`
• Връща `True` ако `expiration_days <= 0`, иначе `False`

#### `decrease_expiration(days: int)`
• Намалява `expiration_days` с подадения брой дни
• Ако стане отрицателно число, да остане 0

---

## 4. Клас BaseShop

Абстрактен базов клас за всички видове магазини.

Не може да бъде инстанциран директно.

### Атрибути:
• `name`: str
  - Име на магазина.
  - Ако името е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Shop name cannot be empty!"
    ```

• `location`: str
  - Местоположение на магазина.
  - Ако е празен стринг или съдържа само интервали, да се вдигне `ValueError` със съобщение:
    ```
    "Shop location cannot be empty!"
    ```

• `capacity`: int
  - Максимален брой продукти, които магазинът може да съдържа.
  - Ако капацитетът е по-малък или равен на 0, да се вдигне `ValueError` със съобщение:
    ```
    "Shop capacity must be a positive number!"
    ```

• `products`: list
  - Празен списък, съдържащ продуктите в магазина.

### Методи:

#### `__init__(name: str, location: str, capacity: int)`
• Инициализира всички атрибути.

#### `@abstractmethod`
#### `shop_type() → str`
Абстрактен метод, който всеки конкретен магазин е длъжен да имплементира.

Цел:
• Да връща типа на магазина като стринг (например "TechShop" или "GroceryShop").
• Използва се при:
  - визуализация (`__str__`)
  - проверки за съвместимост
  - логика в контролния клас

#### `add_product(product: BaseProduct) → str`
Добавя продукт към магазина.

Поведение:
• Ако магазинът няма капацитет, да се вдигне `Exception` със съобщение:
  ```
  "Not enough capacity in the shop!"
  ```
• В противен случай:
  - Добавя продукта към списъка `products`
  - Връща съобщение:
    ```
    "Product {product_name} added to {shop_name}."
    ```

#### `remove_product(product_name: str) → str`
Премахва продукт от магазина.

Поведение:
• Ако продуктът не съществува в магазина, да се вдигне `Exception` със съобщение:
  ```
  "Product {product_name} not found in {shop_name}!"
  ```
• В противен случай:
  - Премахва продукта от списъка
  - Връща съобщение:
    ```
    "Product {product_name} removed from {shop_name}."
    ```

#### `details() → str`
Връща информация за магазина.

Формат:
```
{shop_name} ({shop_type}) at {location}:
Products: {product_name1}, {product_name2}, ..., {product_nameN}
```

• Ако няма продукти:
```
Products: none
```

---

## 5. Клас TechShop

Наследява `BaseShop`.

### Характеристики:
• Капацитет: 50 продукта
• Използва се само за `ElectronicsProduct`

### Имплементация:

#### `__init__(name: str, location: str)`
• Подава капацитет 50 към базовия клас.

#### `shop_type() → str`
• Връща `"TechShop"`

---

## 6. Клас GroceryShop

Наследява `BaseShop`.

### Характеристики:
• Капацитет: 100 продукта
• Използва се само за `FoodProduct`

### Имплементация:

#### `__init__(name: str, location: str)`
• Подава капацитет 100 към базовия клас.

#### `shop_type() → str`
• Връща `"GroceryShop"`

---

## 7. Клас ShopManagementApp

Класът `ShopManagementApp` е основният контролен център на системата. Той отговаря за създаването и управлението на продукти, магазини, разпределянето им, продажбите и изчисляването на общата стойност на стоките в магазините.

### Структура

Класът трябва да има следните атрибути:
• `products`: list
  - Празен списък, който съдържа всички създадени продукти (обекти).

• `shops`: list
  - Празен списък, който съдържа всички създадени магазини (обекти).

### Методи:

#### `__init__()`
Инициализира празните колекции за продукти и магазини.

---

#### `add_product(product_type: str, name: str, price: float, *args)`
Методът създава продукт от подадения тип и го добавя към системата.

Изисквания:
• Всички имена на продукти са уникални.
• Позволени типове продукти са:
  - `"ElectronicsProduct"`
  - `"FoodProduct"`

Поведение:
• Ако подаденият `product_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid product type!"
  ```
• В противен случай:
  - Създава се съответният продукт
  - За `ElectronicsProduct`: `ElectronicsProduct(name, price)`
  - За `FoodProduct`: `FoodProduct(name, price, expiration_days)` където `expiration_days` е от `args`
  - Добавя се към списъка `products`
  - Връща се съобщение:
    ```
    "{product_type} is successfully added."
    ```

---

#### `add_shop(shop_type: str, name: str, location: str)`
Методът създава магазин от подадения тип и го добавя към системата.

Изисквания:
• Всички имена на магазини са уникални.
• Позволени типове магазини:
  - `"TechShop"`
  - `"GroceryShop"`

Поведение:
• Ако подаденият `shop_type` не е валиден, се вдига `Exception` със съобщение:
  ```
  "Invalid shop type!"
  ```
• В противен случай:
  - Създава се съответният магазин
  - Добавя се към списъка `shops`
  - Връща се съобщение:
    ```
    "{shop_type} is successfully added."
    ```

---

#### `stock_product(product_name: str, shop_name: str)`
Методът разпределя продукт към магазин (добавя го в склада на магазина).

Изисквания:
• Продуктът и магазинът винаги съществуват в системата.
• Всеки тип продукт може да бъде добавян само към определен тип магазин:
  - `ElectronicsProduct` → `TechShop`
  - `FoodProduct` → `GroceryShop`

Поведение:
• Ако продуктът не е съвместим с магазина, методът връща:
  ```
  "Incompatible product type for this shop."
  ```
• Ако магазинът няма капацитет, се вдига `Exception` със съобщение:
  ```
  "Not enough capacity for this product!"
  ```
• При успешно добавяне:
  - Продуктът се премахва от списъка `products`
  - Добавя се към магазина
  - Връща се съобщение:
    ```
    "Successfully stocked {product_name} in {shop_name}."
    ```

---

#### `sell_product(shop_name: str, product_name: str, amount: int)`
Методът продава определено количество от продукт, намиращ се в даден магазин.

Изисквания:
• Магазинът винаги съществува.
• Продуктът винаги съществува в магазина.

Поведение:
• Извиква метода `sell(amount)` на продукта
• Връща резултата от метода `sell()`

---

#### `restock_product(shop_name: str, product_name: str, amount: int)`
Методът презарежда количеството на продукт в даден магазин.

Изисквания:
• Магазинът винаги съществува.
• Продуктът винаги съществува в магазина.

Поведение:
• Извиква метода `restock(amount)` на продукта
• Връща резултата от метода `restock()`

---

#### `total_value(shop_name: str) → str`
Методът изчислява общата стойност на продуктите в даден магазин.

Изисквания:
• Магазинът винаги съществува.

Поведение:
• Сумира цените на всички продукти в магазина, умножени по техните количества:
  ```
  total = sum(product.price * product.quantity for product in shop.products)
  ```
• Връща резултата, форматиран до втория знак след десетичната запетая:
  ```
  "Total value of {shop_name}: ${total:.2f}"
  ```

---

#### `__str__() → str`
Методът връща информация за всички магазини в системата.

Формат:
```
{shop_name1} ({shop_type1}) at {location1}:
Products: {product_name1}, {product_name2}, ..., {product_nameN}
{shop_name2} ({shop_type2}) at {location2}:
Products: none
...
```

---

## Примерен изход при тестване

```python
# Създаване на приложението
app = ShopManagementApp()

# Добавяне на продукти
print(app.add_product("ElectronicsProduct", "Laptop", 1500.0))
# ElectronicsProduct is successfully added.

print(app.add_product("FoodProduct", "Bread", 2.5, 7))
# FoodProduct is successfully added.

# Добавяне на магазини
print(app.add_shop("TechShop", "Tech World", "Sofia"))
# TechShop is successfully added.

print(app.add_shop("GroceryShop", "Fresh Market", "Plovdiv"))
# GroceryShop is successfully added.

# Разпределяне на продукти към магазини
print(app.stock_product("Laptop", "Tech World"))
# Successfully stocked Laptop in Tech World.

print(app.stock_product("Bread", "Fresh Market"))
# Successfully stocked Bread in Fresh Market.

# Опит за несъвместимо разпределяне
print(app.stock_product("Bread", "Tech World"))
# Incompatible product type for this shop.

# Продажба на продукти
print(app.sell_product("Tech World", "Laptop", 10))
# Sold 10 units of Laptop.

# Презареждане на продукти
print(app.restock_product("Fresh Market", "Bread", 50))
# Restocked 50 units of Bread.

# Обща стойност на магазин
print(app.total_value("Tech World"))
# Total value of Tech World: $60000.00

# Информация за всички магазини
print(app)
# Tech World (TechShop) at Sofia:
# Products: Laptop
# Fresh Market (GroceryShop) at Plovdiv:
# Products: Bread
```

---

## Допълнителни указания

• Използвайте `abc.ABC` и `@abstractmethod` за абстрактните класове и методи.
• Следете внимателно форматирането на изходните съобщения.
• Валидациите трябва да вдигат точно описаните изключения със съответните съобщения.
• Не забравяйте да имплементирате всички методи и атрибути според спецификацията.

---

**Успех с решението! 🚀**
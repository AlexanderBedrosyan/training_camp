# База данни за начинаещи - Първа лекция

## Какво е База данни?

Представете си база данни като **електронна тетрадка** или **табела в Excel**, където:
- Всеки ред е като една картичка с информация за един човек/предмет
- Всяка колона съдържа определен тип информация (име, възраст, телефон)
- Всичко е организирано и лесно за намиране

### Как изглежда база данни? 

Ето как би изглеждала ученическа база данни:

| ID | Име     | Фамилия  | Възраст | Клас |
|----|---------|----------|---------|------|
| 1  | Иван    | Петров   | 16      | 10   |
| 2  | Мария   | Иванова  | 17      | 11   |
| 3  | Георги  | Стоянов  | 15      | 9    |

### Защо използваме база данни вместо Excel файл?

��� **Проблемът с Excel:**
- Ако програмата се затвори - данните изчезват
- Само един човек може да работи с файла едновременно
- Трудно е да търсим и филтрираме големи количества данни

✅ **Решението - База данни:**
- Данните се пазят завинаги
- Много хора могат да работят едновременно
- Много бързо търсене и филтриране
- Автоматично организиране на информацията

### Какво е Django?

Django е като **преводач** между Python и базата данни. Вместо да учим сложен код за бази данни, Django ни позволява да:
- Пишем обикновен Python код
- Django автоматично го превръща в команди за базата данни
- Работим с данните като с Python обекти

**Пример:** Вместо да пишем сложни команди, просто казваме:
```python
student = Student(name="Иван", age=16)
student.save()  # Django запазва в базата данни
```

## Какво е Django Модел?

**Модел = План за таблица в базата данни**

Мислете за модела като за **чертеж на къща**:
- Чертежът показва къде са стаите, вратите, прозорците
- Моделът показва какви данни ще пазим и от какъв тип са

### Най-простият модел:
```python
from django.db import models

class Student(models.Model):
    name = models.CharField(max_length=50)    # Име - текст
    age = models.IntegerField()               # Възраст - число
```

**Обяснение:**
- `Student` е името на нашата таблица
- `name` е колона за текст (максимум 50 букви)
- `age` е колона за числа

## Само 2 вида данни за начало

Със 2 вида полета можем да направим много неща!

### 1️⃣ CharField - за текст
```python
name = models.CharField(max_length=50)
```
**Какво пази:** Букви, числа, специални символи
**Примери:** "Иван", "Петров", "ivan@abv.bg", "0888123456"
**Задължително:** max_length=50 (колко максимум символа)

### 2️⃣ IntegerField - за числа
```python
age = models.IntegerField()
```
**Какво пази:** Цели числа (без дробна част)
**Примери:** 16, 25, 100, -5, 0
**Не може:** 16.5, "16", True

### Ключови разлики:

| CharField | IntegerField |
|-----------|-------------|
| Пази текст | Пази числа |
| Трябва max_length | Не трябва max_length |
| "Алиса" | 16 |
| Може да сортираме по азбука | Може да смятаме, сравняваме |

#### IntegerField
- **Предназначение**: Съхранява цели числа
- **Тип в базата данни**: INTEGER
- **Диапазон**: -2,147,483,648 до 2,147,483,647
- **Случаи на употреба**: Възраст, брой, ID номера, резултати

```python
# Примери за употреба на IntegerField
age = models.IntegerField()                      # Възраст на човек
score = models.IntegerField()                    # Резултат от тест
quantity = models.IntegerField()                 # Количество артикули
year = models.IntegerField()                     # Година
```

### Други важни типове полета
- `DateField()`: За дати (ГГГГ-ММ-ДД)
- `DateTimeField()`: За дата и час
- `BooleanField()`: За True/False стойности
- `DecimalField()`: За точни десетични числа
- `EmailField()`: За имейл адреси (валидира формата)
- `TextField()`: За голямо текстово съдържание

## Изграждане на първите ви Модели

### Стъпка 1: Прост модел Student
```python
from django.db import models

class Student(models.Model):
    first_name = models.CharField(max_length=50)
    last_name = models.CharField(max_length=50)
    age = models.IntegerField()
    student_id = models.CharField(max_length=10)
    
    def __str__(self):
        return f"{self.first_name} {self.last_name}"
```

### Стъпка 2: Модел Course
```python
class Course(models.Model):
    name = models.CharField(max_length=100)
    code = models.CharField(max_length=10)
    credits = models.IntegerField()
    description = models.TextField()
    
    def __str__(self):
        return f"{self.code}: {self.name}"
```

### Стъпка 3: Добавяне на отношения
```python
class Enrollment(models.Model):
    student = models.ForeignKey(Student, on_delete=models.CASCADE)
    course = models.ForeignKey(Course, on_delete=models.CASCADE)
    grade = models.IntegerField(null=True, blank=True)
    enrollment_date = models.DateField()
    
    def __str__(self):
        return f"{self.student} записан в {self.course}"
```

## Що прави Django от нашия модел?

Когато Django види нашия модел, автоматично създава таблица в базата данни:

| ID | name | age |
|----|------|-----|
| 1  | Иван Петров | 16 |
| 2  | Мария Иванова | 17 |

**Автоматично добавя:**
- `ID` колона (уникален номер за всеки запис)
- Методи за създаване, четене, обновяване, изтриване
- Проверки за правилност на данните

## Как да "преведем" модела в база данни?

### Миграции - как Django прави таблици

Мислете на миграциите като **инструкции за сглобяване на мебели**:
1. Ние пишем модела (= планът на мебелта)
2. Django прави инструкция как да създаде таблицата
3. След това Django изпълнява инструкцията и прави таблицата

### Командите, които трябва да помните:
```bash
# 1. Прави инструкция
python manage.py makemigrations

# 2. Изпълнява инструкцията
python manage.py migrate
```

**Важно:** Винаги след промяна в модела - `makemigrations` и `migrate`!

## Работа с данни - как да добавяме студенти?

### Метод 1: Създаваме като обект
```python
# Създаваме
student = Student()
student.name = "Иван Петров"
student.age = 16
student.save()  # Това го запазва в базата данни

print(f"Новият студент има ID: {student.id}")
```

### Метод 2: По-кратко създаване
```python
# Всичко наведнъж - по-бързо!
student = Student.objects.create(
    name="Мария Иванова", 
    age=17
)
print(f"Създадох студент: {student}")
```

### Как да видим всички студенти?
```python
# Взимаме всички студенти
all_students = Student.objects.all()

# Печатаме ги
for student in all_students:
    print(f"Име: {student.name}, Възраст: {student.age}")
```

### Как да намерим конкретен студент?
```python
# По ID
student = Student.objects.get(id=1)

# По име
student = Student.objects.get(name="Иван Петров")

# Всички на 16 години
sixteen_year_olds = Student.objects.filter(age=16)
```

# Метод 2: Директно създаване
student = Student.objects.create(
## Практически примери за упражнение

### Пример 1: Създаваме 3 студента
```python
# Студент 1
student1 = Student.objects.create(name="Иван Петров", age=16)

# Студент 2  
student2 = Student.objects.create(name="Мария Иванова", age=17)

# Студент 3
student3 = Student.objects.create(name="Георги Стоянов", age=15)
```

### Пример 2: Намираме студенти на определена възраст
```python
# Всички студенти на 16 години
sixteen_year_olds = Student.objects.filter(age=16)
print(f"Намерих {sixteen_year_olds.count()} студента на 16 години")

# Студенти по-млади от 17
younger_students = Student.objects.filter(age__lt=17)
for student in younger_students:
    print(f"{student.name} е на {student.age} години")
```

### Пример 3: Променяме възрастта на студент
```python
# Намиране и промяна
student = Student.objects.get(name="Иван Петров")
student.age = 17  # Иван стана по-голям
student.save()
print(f"{student.name} вече е на {student.age} години")
```

## Какво научихме днес?

✅ **База данни** = електронна тетрадка с таблици  
✅ **Django Модел** = план за таблица  
✅ **CharField** = за текст (трябва max_length)  
✅ **IntegerField** = за цели числа  
✅ **makemigrations** + **migrate** = прави таблицата в базата данни  
✅ **.save()** = запазва в базата данни  
✅ **.objects.all()** = взима всички записи  
✅ **.objects.get()** = взима един запис  
✅ **.objects.filter()** = търси определени записи  

## Домашна работа

1. Създайте модел `Book` с полета `title` (CharField) и `pages` (IntegerField)
2. Направете миграции
3. Създайте 5 книги с различен брой страници
4. Намерете всички книги с повече от 200 страници

## Следваща лекция - Какво ще научим?

В следващата лекция ще разгледаме:
- Повече видове полета (DateField, BooleanField, EmailField)
- Как да правим избори в полетата (choices)
- Как да свърземе таблици една с друга (отношения)
- Django Admin интерфейс за управление на данни
- По-сложни заявки и филтри

**Подгответе се за:** Библиотечна система с книги, автори и читатели! 📚

## Полезни команди за запомняне

```bash
# Създаване на Django проект
django-admin startproject myproject

# Създаване на приложение
python manage.py startapp myapp

# Миграции
python manage.py makemigrations
python manage.py migrate

# Django shell за тестване
python manage.py shell
```